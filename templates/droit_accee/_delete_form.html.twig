<form method="post" action="{{ path('droit_accee_delete', {'id': droit_accee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ droit_accee.id) }}">
    <button class="btn">Delete</button>
</form>
