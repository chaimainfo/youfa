{% extends 'base.html.twig' %}
{% block content %}

    <div class="col-lg-6 col-md-12">
        <div class="card">
            <div class="card-header card-header-warning">
                <h4 class="card-title">Créer un nouveau composant</h4>
                <!--<p class="card-category">New employees on 15th September, 2016</p>-->
            </div>
            <div class="card-body table-responsive">

                {{ include('composant/_form.html.twig') }}
                <a class="demo-google-material-icon" href="{{ path('composant_index') }}">
                    <i style="cursor: pointer;" class="material-icons"> keyboard_backspace</i>
                </a>

            </div>
        </div>

    </div>


{% endblock %}